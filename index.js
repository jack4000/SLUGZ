(function(){

//library to check class existence
function hasClass(el, cls) {
  return el.className && new RegExp("(\\s|^)" + cls + "(\\s|$)").test(el.className);
}

//globals
var slugs = 0;
var pounds = 0;

console.log(upgrades.dog.eyes);

})();
